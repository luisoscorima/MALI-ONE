diff --git a/dist/index.js b/dist/index.js
index 3b13c604409ab8914224842986740a46008677e3..ef3cc060d569ac2884dc7ad493d4824f661b4648 100644
--- a/dist/index.js
+++ b/dist/index.js	
@@ -155,6 +155,8 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        // moveTo evita lineTo implícito de arc() entre módulos del mismo path
+        context.moveTo(size / 2, 0);
         context.arc(0, 0, size / 2, 0, Math.PI * 2);
       }
     });
@@ -164,7 +166,9 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
-        context.rect(-size / 2, -size / 2, size, size);
+        // solape mínimo para evitar hairlines por antialiasing entre módulos vecinos
+        const o = 0.35;
+        context.rect(-size / 2 - o, -size / 2 - o, size + o * 2, size + o * 2);
       }
     });
   }
@@ -174,6 +178,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(0, -size / 2);
         context.arc(0, 0, size / 2, -Math.PI / 2, Math.PI / 2);
         context.lineTo(-size / 2, size / 2);
         context.lineTo(-size / 2, -size / 2);
@@ -187,6 +192,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(0, -size / 2);
         context.arc(0, 0, size / 2, -Math.PI / 2, 0);
         context.lineTo(size / 2, size / 2);
         context.lineTo(-size / 2, size / 2);
@@ -201,6 +207,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(-size / 2, -size / 2);
         context.arc(-size / 2, size / 2, size, -Math.PI / 2, 0);
         context.lineTo(-size / 2, size / 2);
         context.lineTo(-size / 2, -size / 2);
@@ -212,6 +219,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(0, -size / 2);
         context.arc(0, 0, size / 2, -Math.PI / 2, 0);
         context.lineTo(size / 2, size / 2);
         context.lineTo(0, size / 2);
@@ -226,7 +234,9 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(-size / 2, -size / 2);
         context.arc(-size / 2, size / 2, size, -Math.PI / 2, 0);
+        context.moveTo(size / 2, size / 2);
         context.arc(size / 2, -size / 2, size, Math.PI / 2, Math.PI);
       }
     });
@@ -403,7 +413,9 @@ var QRCornerSquare = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(size / 2, 0);
         context.arc(0, 0, size / 2, 0, Math.PI * 2);
+        context.moveTo(size / 2 - dotSize, 0);
         context.arc(0, 0, size / 2 - dotSize, 0, Math.PI * 2);
       }
     });
@@ -496,6 +508,7 @@ var QRCornerDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(size / 2, 0);
         context.arc(0, 0, size / 2, 0, Math.PI * 2);
       }
     });
@@ -753,7 +766,7 @@ var QRCanvas = class {
     this.drawBackground();
     this.drawDots((i, j) => {
       if (this._options.imageOptions.hideBackgroundDots) {
-        if (j >= (count - drawImageSize.hideXDots) / 2 && j < (count + drawImageSize.hideXDots) / 2 && i >= (count - drawImageSize.hideYDots) / 2 && i < (count + drawImageSize.hideYDots) / 2) {
+        if (i >= (count - drawImageSize.hideXDots) / 2 && i < (count + drawImageSize.hideXDots) / 2 && j >= (count - drawImageSize.hideYDots) / 2 && j < (count + drawImageSize.hideYDots) / 2) {
           return false;
         }
       }
@@ -812,26 +825,6 @@ var QRCanvas = class {
     const xBeginning = Math.floor((options.width - count * dotSize) / 2);
     const yBeginning = Math.floor((options.height - count * dotSize) / 2);
     const dot = new QRDot({ context: canvasContext, type: options.dotsOptions.type });
-    canvasContext.beginPath();
-    for (let i = 0; i < count; i++) {
-      for (let j = 0; j < count; j++) {
-        if (filter && !filter(i, j)) {
-          continue;
-        }
-        if (!this._qr.isDark(i, j)) {
-          continue;
-        }
-        const x = yBeginning + j * dotSize;
-        const y = xBeginning + i * dotSize;
-        dot.draw(x, y, dotSize, (xOffset, yOffset) => {
-          if (j + xOffset < 0 || i + yOffset < 0 || j + xOffset >= count || i + yOffset >= count)
-            return false;
-          if (filter && !filter(j + xOffset, i + yOffset))
-            return false;
-          return !!this._qr && this._qr.isDark(i + yOffset, j + xOffset);
-        });
-      }
-    }
     if (options.dotsOptions.gradient) {
       const gradientOptions = options.dotsOptions.gradient;
       const gradient = this._createGradient({
@@ -849,7 +842,29 @@ var QRCanvas = class {
     } else if (options.dotsOptions.color) {
       canvasContext.fillStyle = canvasContext.strokeStyle = options.dotsOptions.color;
     }
-    canvasContext.fill("evenodd");
+    // Un path+fill por módulo evita líneas fantasma entre arcs (rounded/classy)
+    for (let i = 0; i < count; i++) {
+      for (let j = 0; j < count; j++) {
+        if (filter && !filter(i, j)) {
+          continue;
+        }
+        if (!this._qr.isDark(i, j)) {
+          continue;
+        }
+        const x = yBeginning + j * dotSize;
+        const y = xBeginning + i * dotSize;
+        canvasContext.beginPath();
+        const overlap = 0.85;
+        dot.draw(x - overlap / 2, y - overlap / 2, dotSize + overlap, (xOffset, yOffset) => {
+          if (j + xOffset < 0 || i + yOffset < 0 || j + xOffset >= count || i + yOffset >= count)
+            return false;
+          if (filter && !filter(i + yOffset, j + xOffset))
+            return false;
+          return !!this._qr && this._qr.isDark(i + yOffset, j + xOffset);
+        });
+        canvasContext.fill("nonzero");
+      }
+    }
   }
   drawCorners(filter) {
     if (!this._qr) {
diff --git a/dist/index.mjs b/dist/index.mjs
index 227976935c70804929f6e5f1279a57ad57f7f077..53e059e0a4fa467afd7f21ab6ea41314690b4dd4 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs	
@@ -112,6 +112,8 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        // moveTo evita lineTo implícito de arc() entre módulos del mismo path
+        context.moveTo(size / 2, 0);
         context.arc(0, 0, size / 2, 0, Math.PI * 2);
       }
     });
@@ -121,7 +123,9 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
-        context.rect(-size / 2, -size / 2, size, size);
+        // solape mínimo para evitar hairlines por antialiasing entre módulos vecinos
+        const o = 0.35;
+        context.rect(-size / 2 - o, -size / 2 - o, size + o * 2, size + o * 2);
       }
     });
   }
@@ -131,6 +135,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(0, -size / 2);
         context.arc(0, 0, size / 2, -Math.PI / 2, Math.PI / 2);
         context.lineTo(-size / 2, size / 2);
         context.lineTo(-size / 2, -size / 2);
@@ -144,6 +149,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(0, -size / 2);
         context.arc(0, 0, size / 2, -Math.PI / 2, 0);
         context.lineTo(size / 2, size / 2);
         context.lineTo(-size / 2, size / 2);
@@ -158,6 +164,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(-size / 2, -size / 2);
         context.arc(-size / 2, size / 2, size, -Math.PI / 2, 0);
         context.lineTo(-size / 2, size / 2);
         context.lineTo(-size / 2, -size / 2);
@@ -169,6 +176,7 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(0, -size / 2);
         context.arc(0, 0, size / 2, -Math.PI / 2, 0);
         context.lineTo(size / 2, size / 2);
         context.lineTo(0, size / 2);
@@ -183,7 +191,9 @@ var QRDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(-size / 2, -size / 2);
         context.arc(-size / 2, size / 2, size, -Math.PI / 2, 0);
+        context.moveTo(size / 2, size / 2);
         context.arc(size / 2, -size / 2, size, Math.PI / 2, Math.PI);
       }
     });
@@ -360,7 +370,9 @@ var QRCornerSquare = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(size / 2, 0);
         context.arc(0, 0, size / 2, 0, Math.PI * 2);
+        context.moveTo(size / 2 - dotSize, 0);
         context.arc(0, 0, size / 2 - dotSize, 0, Math.PI * 2);
       }
     });
@@ -453,6 +465,7 @@ var QRCornerDot = class {
     this._rotateFigure({
       ...args,
       draw: () => {
+        context.moveTo(size / 2, 0);
         context.arc(0, 0, size / 2, 0, Math.PI * 2);
       }
     });
@@ -713,7 +726,7 @@ var QRCanvas = class {
     this.drawBackground();
     this.drawDots((i, j) => {
       if (this._options.imageOptions.hideBackgroundDots) {
-        if (j >= (count - drawImageSize.hideXDots) / 2 && j < (count + drawImageSize.hideXDots) / 2 && i >= (count - drawImageSize.hideYDots) / 2 && i < (count + drawImageSize.hideYDots) / 2) {
+        if (i >= (count - drawImageSize.hideXDots) / 2 && i < (count + drawImageSize.hideXDots) / 2 && j >= (count - drawImageSize.hideYDots) / 2 && j < (count + drawImageSize.hideYDots) / 2) {
           return false;
         }
       }
@@ -772,26 +785,6 @@ var QRCanvas = class {
     const xBeginning = Math.floor((options.width - count * dotSize) / 2);
     const yBeginning = Math.floor((options.height - count * dotSize) / 2);
     const dot = new QRDot({ context: canvasContext, type: options.dotsOptions.type });
-    canvasContext.beginPath();
-    for (let i = 0; i < count; i++) {
-      for (let j = 0; j < count; j++) {
-        if (filter && !filter(i, j)) {
-          continue;
-        }
-        if (!this._qr.isDark(i, j)) {
-          continue;
-        }
-        const x = yBeginning + j * dotSize;
-        const y = xBeginning + i * dotSize;
-        dot.draw(x, y, dotSize, (xOffset, yOffset) => {
-          if (j + xOffset < 0 || i + yOffset < 0 || j + xOffset >= count || i + yOffset >= count)
-            return false;
-          if (filter && !filter(j + xOffset, i + yOffset))
-            return false;
-          return !!this._qr && this._qr.isDark(i + yOffset, j + xOffset);
-        });
-      }
-    }
     if (options.dotsOptions.gradient) {
       const gradientOptions = options.dotsOptions.gradient;
       const gradient = this._createGradient({
@@ -809,7 +802,29 @@ var QRCanvas = class {
     } else if (options.dotsOptions.color) {
       canvasContext.fillStyle = canvasContext.strokeStyle = options.dotsOptions.color;
     }
-    canvasContext.fill("evenodd");
+    // Un path+fill por módulo evita líneas fantasma entre arcs (rounded/classy)
+    for (let i = 0; i < count; i++) {
+      for (let j = 0; j < count; j++) {
+        if (filter && !filter(i, j)) {
+          continue;
+        }
+        if (!this._qr.isDark(i, j)) {
+          continue;
+        }
+        const x = yBeginning + j * dotSize;
+        const y = xBeginning + i * dotSize;
+        canvasContext.beginPath();
+        const overlap = 0.85;
+        dot.draw(x - overlap / 2, y - overlap / 2, dotSize + overlap, (xOffset, yOffset) => {
+          if (j + xOffset < 0 || i + yOffset < 0 || j + xOffset >= count || i + yOffset >= count)
+            return false;
+          if (filter && !filter(i + yOffset, j + xOffset))
+            return false;
+          return !!this._qr && this._qr.isDark(i + yOffset, j + xOffset);
+        });
+        canvasContext.fill("nonzero");
+      }
+    }
   }
   drawCorners(filter) {
     if (!this._qr) {
